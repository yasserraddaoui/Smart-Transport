# Centralized API base URL (talk to API Gateway only)
# - In dev, keep it "/" to use Vite proxy to the gateway
# - In production, it should be the same origin where the gateway serves the frontend
VITE_API_BASE_URL=/

# Optional: override the gateway proxy target for local dev (vite.config.ts)
VITE_GATEWAY_PROXY_TARGET=http://localhost:8080

